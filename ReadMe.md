# How to use
```	
	// will create a .link folder
	cargo-link init
	// will act like ls but just list every dir or file name under that path
	cargo-link <path>
	// will create a symlink of said path and put it in your project
	cargo-link <path> register <the-picked-dir-or-file>
```
